# Data & model artifacts (regenerated by the pipeline, too big for git)
data/*.parquet
data/*.npy
data/raw/
outputs/*.png
outputs/*.html
outputs/*.csv
# ...but keep the showcase figures + interactive map so they're viewable on GitHub.
!outputs/landscape.png
!outputs/gap_map.png
!outputs/landscape.html

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
